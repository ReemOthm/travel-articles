export type Article = {
    id: string,
    slug: string,
    articleName: string,
    articleContent: string[],
    articleImages: string[]
}

export type Titles = {
    title: string, 
    slug: string
}